{
  "name": "@valiantlynx/tsconfig",
  "version": "0.0.11",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp base.json nextjs.json react-library.json ts-library.json svelte.json noentry.js dist/",
    "changeset": "changeset",
    "publish:packages": "changeset publish",
    "version:packages": "changeset version",
    "release": "yarn publish:packages && yarn version:packages"
  },
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/noentry.js",
  "devDependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
